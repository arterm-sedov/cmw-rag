# Static model metadata registry for cmw-rag
# This file contains model specifications, dimensions, and provider-specific formatting rules
# Runtime configuration (which model/provider to use) goes in .env

models:
  # ============ EMBEDDING MODELS ============

  ai-forever/FRIDA:
    type: embedding
    dimensions: 1536
    description: "Russian-optimized multilingual embeddings"
    # FRIDA uses query_prefix/doc_prefix (no instruction needed)
    provider_formats:
      direct:
        query_prefix: "search_query: "
        doc_prefix: "search_document: "
        device: auto
        max_seq_length: 512
      infinity:
        query_prefix: "search_query: "
        doc_prefix: "search_document: "
      mosec:
        query_prefix: "search_query: "
        doc_prefix: "search_document: "
      vllm:
        supported: false
      openrouter:
        supported: false

  Qwen/Qwen3-Embedding-0.6B:
    type: embedding
    dimensions: 1024
    description: "Lightweight Qwen3 embedding model (1B params)"
    default_instruction: "Given a web search query, retrieve relevant passages that answer the query"
    provider_formats:
      direct:
        device: auto
        max_seq_length: 8192
      vllm:
        supported: false
      openrouter:
        supported: false

  Qwen/Qwen3-Embedding-4B:
    type: embedding
    dimensions: 2560
    description: "Mid-size Qwen3 embedding model (4B params)"
    default_instruction: "Given a web search query, retrieve relevant passages that answer the query"
    provider_formats:
      direct:
        device: auto
        max_seq_length: 8192
      openrouter:
        model_id: "qwen/qwen3-embedding-4b"

  Qwen/Qwen3-Embedding-8B:
    type: embedding
    dimensions: 4096
    description: "Large Qwen3 embedding model (8B params, best quality)"
    default_instruction: "Найди релевантную техническую документацию"
    provider_formats:
      direct:
        device: auto
        max_seq_length: 8192
      openrouter:
        model_id: "qwen/qwen3-embedding-8b"

  # ============ RERANKER MODELS ============

  DiTy/cross-encoder-russian-msmarco:
    type: reranker
    reranker_type: cross_encoder
    dimensions: 1
    description: "Russian-optimized cross-encoder reranker"
    provider_formats:
      direct:
        batch_size: 16
        device: auto
      mosec: {}

  BAAI/bge-reranker-v2-m3:
    type: reranker
    reranker_type: cross_encoder
    dimensions: 1
    description: "Multilingual BGE reranker"
    provider_formats:
      direct:
        batch_size: 16
        device: auto
      mosec: {}

  Qwen/Qwen3-Reranker-0.6B:
    type: reranker
    reranker_type: llm_reranker
    dimensions: 1
    description: "Lightweight Qwen3 reranker"
    default_instruction: "Найди релевантную техническую документацию"
    formatting:
      query_template: "{prefix}<Instruct>: {instruction}\n<Query>: {query}\n"
      doc_template: "<Document>: {doc}{suffix}"
      prefix: "<|im_start|>system\nJudge whether the Document meets the requirements based on the Query and the Instruct provided. Note that the answer can only be \"yes\" or \"no\".<|im_end|>\n<|im_start|>user\n"
      suffix: "<|im_end|>\n<|im_start|>assistant\n\n\n\n\n"
    provider_formats:
      direct:
        supported: false
      mosec: {}

  Qwen/Qwen3-Reranker-4B:
    type: reranker
    reranker_type: llm_reranker
    dimensions: 1
    description: "Mid-size Qwen3 reranker"
    default_instruction: "Given a web search query, retrieve relevant passages that answer the query"
    formatting:
      query_template: "{prefix}<Instruct>: {instruction}\n<Query>: {query}\n"
      doc_template: "<Document>: {doc}{suffix}"
      prefix: "<|im_start|>system\nJudge whether the Document meets the requirements based on the Query and the Instruct provided. Note that the answer can only be \"yes\" or \"no\".<|im_end|>\n<|im_start|>user\n"
      suffix: "<|im_end|>\n<|im_start|>assistant\n\n\n\n\n"
    provider_formats:
      direct:
        supported: false
      mosec: {}

  Qwen/Qwen3-Reranker-8B:
    type: reranker
    reranker_type: llm_reranker
    dimensions: 1
    description: "Large Qwen3 reranker"
    default_instruction: "Given a web search query, retrieve relevant passages that answer the query"
    formatting:
      query_template: "{prefix}<Instruct>: {instruction}\n<Query>: {query}\n"
      doc_template: "<Document>: {doc}{suffix}"
      prefix: "<|im_start|>system\nJudge whether the Document meets the requirements based on the Query and the Instruct provided. Note that the answer can only be \"yes\" or \"no\".<|im_end|>\n<|im_start|>user\n"
      suffix: "<|im_end|>\n<|im_start|>assistant\n\n\n\n\n"
    provider_formats:
      direct:
        supported: false
      mosec: {}

  # ============ INFERENCE MODELS ============

  Qwen/Qwen3-8B-Instruct:
    type: llm
    dimensions: 4096
    description: "Qwen3 instruction model with improved reasoning"
    provider_formats:
      openrouter:
        model_id: "qwen/qwen3-8b"

  Qwen/Qwen3-32B-Instruct:
    type: llm
    dimensions: 6144
    description: "Large Qwen3 instruction model"
    provider_formats:
      openrouter:
        model_id: "qwen/qwen3-32b"

  deepseek/deepseek-v3.2-speciale:
    type: llm
    dimensions: 4096
    description: "DeepSeek V3.2 Speciale – high-compute variant for max reasoning and agentic performance (163K context)"
    provider_formats:
      openrouter:
        model_id: "deepseek/deepseek-v3.2-speciale"

  # ============ GUARDIAN (CONTENT MODERATION) MODELS ============

  Qwen/Qwen3Guard-Gen-0.6B:
    type: guard
    dimensions: 1
    description: "Qwen3Guard content safety model (0.6B params, 119 languages)"
    provider_formats:
      vllm: {}
      mosec: {}

  Qwen/Qwen3Guard-Gen-4B:
    type: guard
    dimensions: 1
    description: "Qwen3Guard content safety model (4B params, 119 languages)"
    provider_formats:
      vllm: {}
      mosec: {}

  Qwen/Qwen3Guard-Gen-8B:
    type: guard
    dimensions: 1
    description: "Qwen3Guard content safety model (8B params, 119 languages)"
    provider_formats:
      vllm: {}
      mosec: {}


# Default provider endpoints and settings
defaults:
  endpoints:
    infinity:
      embedding: "http://localhost:7997/v1/embeddings"
      reranker: "http://localhost:7997/v1/rerank"
    mosec:
      embedding: "http://localhost:7998/v1/embeddings"
      reranker: "http://localhost:7998/v1/rerank"
    vllm:
      embedding: "http://localhost:8000/v1/embeddings"
    openrouter: "https://openrouter.ai/api/v1"

  timeouts:
    infinity: 60.0
    mosec: 60.0
    vllm: 60.0
    openrouter: 60.0

  retries:
    infinity: 3
    mosec: 3
    vllm: 3
    openrouter: 3
